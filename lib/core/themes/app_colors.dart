import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  // Brand seed
  static const Color seed = Color(0xFFB0EE2C);

  // Extra custom colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFB800);
  static const Color info = Color(0xFF4DB6AC);

  // Optional custom colors for skincare tags
  static const Color oily = Color(0xFFB8E65C);
  static const Color dry = Color(0xFF81C7BE);
  static const Color sensitive = Color(0xFFFF8A80);

  // Optional scaffold backgrounds
  static const Color lightBackground = Color(0xFFF8F9F0);
  static const Color darkBackground = Color(0xFF11130D);

  // Aura/Mesh Gradient Colors (Brand Adaptive)
  static const Color lime = Color(0xFFF1FCE0);
  static const Color mint = Color(0xFFE0F2F1);
  static const Color yellow = Color(0xFFFEF9E7);
}